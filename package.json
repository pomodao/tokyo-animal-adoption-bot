{
  "name": "tokyo-animal-adoption-bot",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --pretty false",
    "monitor": "node src/cli/run-monitor.ts",
    "test": "find src -name \"*.test.ts\" -print0 | xargs -0 node --test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
